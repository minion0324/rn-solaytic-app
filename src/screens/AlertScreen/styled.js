import styled from 'styled-components';

import {
  COLORS,
  COMMON_SIZE,
  COMMON_PADDING,
} from 'src/constants';

const HelloText = styled.Text`
  font-size: 18px;
  color: ${COLORS.WHITE1};
`;

export {
  HelloText,
};
